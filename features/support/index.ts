export interface ChatbotScreenProps {
  // Add any props that ChatbotScreen might need in the future
}

export interface SellerChatScreenProps {
  // Add any props that SellerChatScreen might need in the future
}

export interface ChatScreenProps {
  // Add any props that ChatScreen might need in the future
}